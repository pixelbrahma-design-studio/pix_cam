import 'package:flutter/cupertino.dart';

class MonthlyDataScreen extends StatelessWidget {
  const MonthlyDataScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Monthly Data Screen"),
    );
  }
}
